name: appone
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # UI animation and effects
  lottie: ^3.1.2
  carousel_slider: ^5.1.1
  animated_text_kit: ^4.2.2
  google_fonts: ^6.2.1
  introduction_screen: ^3.1.12  # 👈 Added for onboarding pages
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/animations/welcome.json
    - assets/animations/organize.json
    - assets/animations/start.json
    - assets/loginn.json
    - assets/write.json
    - assets/music.json
    - assets/travel.json
    - assets/Home.json

